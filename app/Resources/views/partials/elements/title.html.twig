{% if _title_type == 'fancy_title' %}
    <h3 class="formPageHead fancyBorder">
        <span>{{ _content|raw }}</span>
    </h3>
{% elseif _title_type == 'main_title' %}
    <div class="mainTitle fancy">
        <h2>{{ _content|raw }}</h2>
    </div>
{% endif %}